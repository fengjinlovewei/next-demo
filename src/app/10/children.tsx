import Link from 'next/link';

export const metadata = {
  title: '...',
  description: '...',
};

function Home() {
  return (
    <main className='flex flex-row flex-wrap'>
      <div>children</div>
    </main>
  );
}

export default Home;
